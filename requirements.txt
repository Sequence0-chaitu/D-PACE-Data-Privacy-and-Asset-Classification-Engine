# D-PACE Python dependencies

# Core (stdlib only — no third-party deps required for basic operation)
# The following are optional / dev dependencies:

# Testing
pytest>=8.0
pytest-cov>=5.0

# Linting & formatting
ruff>=0.4
mypy>=1.10

# Optional: rich CLI output
rich>=13.7
